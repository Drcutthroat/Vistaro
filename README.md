# Vistaro (Polished UI MVP)

Working local MVP with a polished UI:
- Demo login (roles)
- Modules launcher
- Hall Pass trips with green→red timer + overdue count
- In-app alerts (auto-created when overdue)
- Coins (+3 for on-time returns)

## Run
```bash
npm install
npm run dev
```
Open http://localhost:3000
